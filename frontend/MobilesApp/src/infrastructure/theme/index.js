import { colors } from "./colors";
import { sizes } from "./sizes";
import { lineHeights, space } from "./spacings";
import { fonts, fontSizes, fontWeights } from "./fonts";

export const theme = {
  colors,
  sizes,
  space,
  lineHeights,
  fonts,
  fontSizes,
  fontWeights,
};
